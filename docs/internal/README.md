# 로컬 전용 내부 자료

이 디렉터리는 현장 규격, 센서 설치값, 사설 주소, 실장비 측정 결과와 편집 원본을 로컬에서 관리한다. 이 자료는 읽기 전용이며, 새로운 측정 결과나 보정값은 기존 파일을 덮어쓰지 않고 별도 파일로 추가한다. `.gitignore`는 이 안내 파일을 제외한 나머지 내용을 제외한다.

공개 Repository에서 작업하는 코딩 에이전트는 이 디렉터리의 내용을 commit, push, 로그, 테스트 고정값이나 예시 설정으로 복제하지 않는다. 공개 코드는 실행 시 주입하는 설정으로 현장 값을 받는다.
